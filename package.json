{
  "name": "kmart-ai-kb-pipeline",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "km-kb": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "dev": "ts-node src/cli.ts",
    "pipeline": "npm run build && node dist/pipeline/run.js",
    "clean:processing:build": "tsc -p .",
    "clean:processing:dry": "npm run build && node dist/tools/cleanProcessing.js --dry-run",
    "clean:processing": "npm run build && node dist/tools/cleanProcessing.js --confirm"
  },
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^12.1.0",
    "dotenv": "^17.2.3",
    "fast-glob": "^3.3.2",
    "mammoth": "^1.11.0",
    "openai": "^4.59.0",
    "p-limit": "^6.1.0",
    "pdf-parse": "^2.4.5",
    "tesseract.js": "^6.0.1",
    "textract": "^2.5.0",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
